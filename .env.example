# ── Embedding gateway ─────────────────────────────────────────────────────────
# Required. Update with your actual gateway values before running.

EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
